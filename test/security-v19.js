'use strict'
const fs=require('fs'),path=require('path'),assert=require('assert'),root=path.join(__dirname,'..'),read=f=>fs.readFileSync(path.join(root,f),'utf8');const engine=read('api/_engine-v11.js'),data=read('api/data-v11.js'),migration=read('supabase/migrations/20260920082451_v19_mdllni_public_snapshot_boundary.sql');let passed=0;function test(name,fn){fn();passed++;console.log('✓',name)}test('anonymous path uses dedicated RPC',()=>{assert(engine.includes('mdllni_public_snapshot'));assert(data.includes('viewer?await snapshot(viewer):await publicSnapshot()'))});test('streamed request bodies are capped',()=>{assert(data.includes('size>MAX_BODY'));assert(!data.includes("require('./_lib').readBody"))});test('public snapshot returns no order rows',()=>assert(migration.includes("'orders','[]'::jsonb")));test('public function is stable and security definer',()=>{assert(migration.includes('stable security definer'));assert(migration.includes("set search_path to ''"))});test('public execution is revoked',()=>assert(migration.includes('from public,anon,authenticated')));test('service role retains execution',()=>assert(migration.includes('to service_role')));console.log('\n'+passed+'/'+passed+' v19 security checks passed')
