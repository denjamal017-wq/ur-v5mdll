# مدللني — حزمة الأمان v8.0 / v8.6 (10 أيلول 2026)

## شنو الجديد بهالإصدار
- **تحقق بخطوتين عبر البريد (OTP)**: رمز 6 أرقام للتسجيل والدخول والجهاز الجديد — صالح 10 دقائق، 5 محاولات، دقيقة بين كل إعادة إرسال، حد يومي 8 رموز. **ماكو جلسة قبل رمز البريد.**
- **جهاز واحد = حساب واحد**: الجهاز ينربط بالحساب بفهرس فريد بالقاعدة. تسجيل حساب ثانٍ من نفس الجهاز → مرفوض (`device_in_use`).
- **تبديل التلفون العطلان**: دخول من جهاز جديد → رمز بريد → الجهاز يصير «معلق» → الإدارة تعتمده من تبويب الأمان (تبديل = يلغي القديم ويفعّل الجديد) — بدون ما ينفقد الحساب.
- **إلغاء جهاز يقتل جلسته فوراً**: أي توكن بجهاز ملغى يرجع 401 `device_revoked`.
- **شبكة تواطؤ بآخر IP**: زبون ومقدم بنفس الجهاز **أو نفس آخر عنوان IP** → المقدم يُستبعد من الطلب + الطلب يُعلَّم 🚨 + يُوثَّق بسجل الأحداث الأمنية. (حالة التلفونين على نفس الشبكة)
- **تبويب 🛡️ الأمان الجديد بلوحة الإدارة**: طلبات الأجهزة المعلقة (اعتماد/تبديل/رفض) + جدول كل الأجهزة + سجل الأحداث الأمنية الأخير.
- **بطاقة «أجهزتي»** بصفحة حساب كل مستخدم.
- **Cloudflare Turnstile اختياري** ضد البوتات — يشتغل فقط إذا ضبطت مفاتيحه.
- **العمولة ذرية بالقاعدة** عبر RPC `ur_apply_debt` — سباق الإكمال المزدوج ما يضيّع ولا دينار (مُختبَر).
- إصلاح إيموجي «منطقة الخطر» المتضرر + رسائل خطأ عربية لكل رموز الهوية الجديدة.

## الرفع — مهم جداً: ارفع **داخل** المجلدات (مو بالجذر!)
1. افتح المستودع → ادخل مجلد **`api/`** → Add file → Upload files → ارفع **الملفات الستة** من مجلد `api/` بالحزمة (`_engine.js`, `_lib.js`, `_mail.js`, `auth.js`, `data.js`, `health.js`) — الخمسة القديمة تُستبدل و`_mail.js` جديد. Commit.
2. ادخل مجلد **`js/`** → Upload → ارفع `cloud.js` (يستبدل الموجود). Commit.
3. `test/security-v8.js` اختياري (للتطوير فقط) — ارفعه بمجلد `test/` أو تجاهله؛ Vercel ما يلمسه.
4. `app.js` و`vercel.json` و`package.json` **ما تغيّرت** — لا ترفعها.
5. Vercel ينشر تلقائياً بعد كل push.

## قاعدة البيانات — ✅ مفعّلة من عندي
migration v12 انطبقت على Supabase (جداول `ur_devices` / `ur_email_otps` / `ur_security_events` + أعمدة `profiles.email/email_verified/last_ip` + فهرس البريد الفريد + فهرس جهاز-واحد-فعّال + دالة `ur_apply_debt` الذرية + فهرس منع تكرار العمولة). **ما تحتاج تسوي شي بالقاعدة.**

## متغيرات البيئة الجديدة (Vercel → Project → Settings → Environment Variables)
**البريد — مطلوب حتى يشتغل OTP للحسابات الجديدة** (اختر مزوداً واحداً؛ الأسهل Resend — مجاني 100 رسالة/يوم):
| المتغير | القيمة |
|---|---|
| `MAIL_PROVIDER` | `resend` (أو `sendgrid` / `brevo`) |
| `MAIL_API_KEY` | مفتاح API من لوحة المزود |
| `MAIL_FROM` | `مدللني <otp@yourdomain.com>` — دومين موثّق عند المزود |

بدونها: التسجيل الجديد يرجع `mail_not_configured` والإدارة تتنبه — الحسابات القديمة بلا بريد (ومنها حسابك admin) **تدخل عادي** ما تنقفل.

**Turnstile — اختياري لكن مُوصى به** (مجاني: dash.cloudflare.com → Turnstile → Add site):
| المتغير | القيمة |
|---|---|
| `TURNSTILE_SITE_KEY` | المفتاح العلني |
| `TURNSTILE_SECRET` | المفتاح السري |

بدونهما يتخطى التحقق بهدوء — ما ينكسر شي.

⚠️ `MAIL_DEV_ECHO=1` **للتجربة المحلية فقط — لا تضيفه لـ Vercel أبداً** (يطبع رموز OTP بالسجلات).

## بعد النشر — تحقق بنفسك
1. افتح `/api/health` → لازم `"version":"v8"` و`mailReady:true` (بعد ضبط البريد).
2. سجّل حساباً تجريبياً → يوصلك رمز 6 أرقام ببريدك → أكّد → تدخل.
3. سجّل دخول من متصفح آخر/جوالك → رمز بريد → «ينتظر موافقة الإدارة» → من الإدارة: 🛡️ الأمان → اعتماد وتبديل.
4. جرّب تسجّل حساباً ثانياً من نفس الجهاز → لازم ينرفض.

## حدود صادقة (اقرأها)
- **تلفونان بنفس الوقت**: مستحيل منعها 100% تقنياً — لكن صارت مكشوفة ومكلفة: بريد فريد لكل حساب + تطابق آخر IP يستبعد التواطؤ ويوثّقه + العمولة تاكل ربح التحايل.
- أمان الحساب صار بقوة بريده — نصّح المستخدمين بكلمة مرور قوية للبريد.
- ربط Cloudflare كبروكسي أمام Vercel **ما ننصح به** (Vercel رسمياً ضده: يكسر السرعة والتحليلات) — Turnstile يشتغل بدون أي بروكسي.
- **دوّر فوراً** الأسرار اللي انلصقت بالدردشة: Cloudflare OAuth Client ID/Secret + توكنات Vercel (`vck_…` / `vcp_…`).

## الاختبارات
`node test/security-v8.js` — **38/38 ✅** بدون شبكة (قاعدة وهمية بالذاكرة): OTP كامل، قفل المحاولات، جهاز-واحد، تبديل/إلغاء الأجهزة، قتل الجلسات، شبكة التواطؤ IP، سباق العمولة المزدوج، Turnstile، عزل صلاحيات السنابشوت.
